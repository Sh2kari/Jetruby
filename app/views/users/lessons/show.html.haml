.content
  .row
    .col-md-12
      %h4= @lesson.title
      %p Position
      %p= @lesson.position
      %p Description
      %p= @lesson.description
      %p Cover
      %p= image_tag @lesson.cover.thumb
      %p Summary
      %p= @lesson.summary
      %p Homework
      %p= @lesson.homework

      - if user_signed_in?
        = link_to 'Show Homeworks', course_lesson_homeworks_path(@course, @lesson), class: "btn btn-primary"
